================================================================================
RESUMO DAS CORREÇÕES - PROBLEMA DE CONEXÃO WEBSOCKET NO APK RELEASE
================================================================================

PROBLEMA IDENTIFICADO:
A conexão WebSocket funcionava em debug (flutter run) mas falhava no APK release.
Causas raiz:
1. Falta de configuração de segurança de rede (Network Security Config)
2. Falta de regras ProGuard/R8 para proteger classes críticas
3. Logging desabilitado em builds release, impossibilitando diagnóstico

================================================================================
CORREÇÕES IMPLEMENTADAS:
================================================================================

1. NETWORK SECURITY CONFIG (CRÍTICO)
   Arquivo: mobile/android/app/src/main/res/xml/network_security_config.xml
   
   O que foi feito:
   - Criado arquivo de configuração de segurança de rede
   - Permitido tráfego cleartext (HTTP/WS) para localhost e redes locais
   - Mantido HTTPS/WSS como padrão para produção
   - Configurado suporte a certificados customizados se necessário
   
   Por que funciona:
   - Android 9+ (API 28+) rejeita conexões HTTP/WebSocket não criptografadas por padrão
   - O arquivo permite exceções para desenvolvimento local
   - Referenciado no AndroidManifest.xml via android:networkSecurityConfig

2. PROGUARD/R8 RULES (CRÍTICO)
   Arquivo: mobile/android/app/proguard-rules.pro
   
   O que foi feito:
   - Criado arquivo com regras ProGuard para proteger classes críticas
   - Protegidas classes do Flutter, WebSocket e serialização JSON
   - Habilitada minificação com ProGuard/R8 no build.gradle.kts
   - Mantidos nomes de classes para debugging
   
   Por que funciona:
   - Sem regras ProGuard, classes podem ser ofuscadas ou removidas
   - A biblioteca web_socket_channel pode ter suas classes renomeadas
   - Isso quebra a reflexão e a serialização JSON
   - As regras garantem que classes essenciais sejam preservadas

3. LOGGING MELHORADO (IMPORTANTE)
   Arquivo: mobile/lib/services/logger_service.dart
   
   O que foi feito:
   - Criado serviço de logging que funciona em debug e release
   - Usa dart:developer.log para logging em release builds
   - Mantém print() em debug mode para compatibilidade
   - Substituídos todos os print() e debugPrint() por LoggerService
   
   Arquivos atualizados:
   - lib/services/websocket_service.dart
   - lib/components/ui/realtime_data_widget.dart
   - lib/screens/location_screen.dart
   - lib/screens/map_screen.dart
   
   Por que funciona:
   - print() é removido em builds release
   - dart:developer.log funciona em release e pode ser visualizado com:
     * Android Studio Logcat
     * adb logcat
     * Flutter DevTools
   - Permite diagnóstico de problemas em produção

================================================================================
COMO TESTAR A SOLUÇÃO:
================================================================================

1. GERAR APK RELEASE:
   flutter clean
   flutter build apk --release
   
   O APK será gerado em: build/app/outputs/flutter-apk/app-release.apk

2. INSTALAR NO DISPOSITIVO:
   adb install -r build/app/outputs/flutter-apk/app-release.apk

3. VERIFICAR LOGS:
   adb logcat | grep -E "PetDex|WebSocket|Connection"
   
   Ou usar Android Studio Logcat com filtro "PetDex"

4. TESTAR CONEXÃO WEBSOCKET:
   - Abrir o aplicativo
   - Navegar para a tela de localização
   - Verificar se a conexão WebSocket é estabelecida
   - Observar os logs para mensagens de sucesso/erro

5. LOGS ESPERADOS EM CASO DE SUCESSO:
   ✅ Conectado ao WebSocket
   🔌 Tentando conectar ao WebSocket...
   🔗 URL: wss://seu-servidor/ws-petdex/websocket
   💓 Batimento chegou na interface
   🎯 Localização chegou na interface

6. LOGS ESPERADOS EM CASO DE ERRO:
   ❌ Erro na conexão WebSocket
   ⚠️ Falha ao conectar em [endpoint]
   🔄 Reconexão agendada em Xs (tentativa X/10)

================================================================================
CONFIGURAÇÕES IMPORTANTES:
================================================================================

1. NETWORK SECURITY CONFIG:
   - Editar mobile/android/app/src/main/res/xml/network_security_config.xml
   - Adicionar domínios do seu servidor em <domain-config>
   - Exemplo: <domain includeSubdomains="true">seu-servidor.com</domain>

2. PROGUARD RULES:
   - Se adicionar novas bibliotecas, adicionar regras correspondentes
   - Manter -keep class com.example.mobile.** { *; }
   - Testar com: flutter build apk --release --analyze-size

3. CERTIFICADOS SSL/TLS:
   - Se usar WSS com certificado customizado:
     * Adicionar certificado em android/app/src/main/res/raw/
     * Referenciar em network_security_config.xml
     * Exemplo: <certificates src="@raw/custom_cert" />

================================================================================
TROUBLESHOOTING:
================================================================================

Se a conexão ainda não funcionar:

1. Verificar URL do WebSocket:
   - Confirmar que API_JAVA_URL está correto no .env
   - Testar URL manualmente com curl/Postman
   - Verificar se servidor está rodando

2. Verificar permissões:
   - INTERNET: ✅ Presente em AndroidManifest.xml
   - ACCESS_FINE_LOCATION: ✅ Presente
   - FOREGROUND_SERVICE: ✅ Presente

3. Verificar certificados SSL:
   - Se usar WSS, certificado deve ser válido
   - Testar com: openssl s_client -connect seu-servidor:443

4. Verificar firewall:
   - Porta do WebSocket deve estar aberta
   - Testar com: telnet seu-servidor porta

5. Verificar logs detalhados:
   - adb logcat -v threadtime | grep -E "PetDex|WebSocket|ssl|certificate"
   - Procurar por mensagens de erro específicas

================================================================================
PRÓXIMOS PASSOS:
================================================================================

1. Testar a solução com o APK release
2. Monitorar logs em produção
3. Se necessário, adicionar mais regras ProGuard
4. Considerar adicionar certificado pinning para segurança
5. Implementar retry logic mais robusta se necessário

================================================================================

